# Session — 2026-07-01

Branch: `session/handoff-and-record-trim-fix`. Focus: Sheet confirm, reel-name/brand fixes,
then vendoring + recoloring the FreeCut editor, then organizing docs.

## 1. Google Sheet bridge — confirmed live
- New Apps Script deployed by Dennis; new webhook URL set in `backend/.env`.
- Test push via `sheets.push_perf_rows` → `{pushed: 1, live: True}`; all text fields land
  correctly (the array-loop fix works).
- The 3 "Input must be an item on the specified list" flags were **data-validation dropdowns**
  not matching test values — Dennis removed the dropdowns. Non-issue.
- TODO (Dennis): delete stray test rows (`clip_18`, `clip_1`, `__CVIDEO TEST__`).

## 2. Reel-name unification
- New `_video_name` / `_is_reel_clip` in `main.py`: a reel is a caption-mode project's clip,
  named by the **project**. Wired into `_video_label` (Results/Sheet), `list_exports`
  (Downloads), `insights` (`is_reel`).
- Downloads: reels now group under one **Reels** folder split into **brand** subfolders; plain
  clips unchanged. Frontend `VideoTracker` shows `v.title` (canonical name).
- Verified via API + live DOM: "Bread 2" etc. now consistent everywhere.

## 3. Brand selection + backfill
- Added `Project.brand` (+ migration). Choose brand at **New project** (caption mode) and **New
  video** (was hardcoded NoCrapDiet). Results brand dropdown now **saves on change** (no metrics
  needed) via new `POST /api/videos/{kind}/{id}/brand`.
- `_video_brand` resolves `Clip.brand → Project.brand → linked Ticket → blank`.
- **One-time backfill**: all existing caption reels → NoCrapDiet (gated on the column being
  freshly added). Verified: 15 reels show NoCrapDiet; plain clip stays blank.
- Files: `db.py`, `main.py`, `api.ts`, `App.tsx`.

## 4. FreeCut editor — vendored, running, recolored
- Decision: adopt **FreeCut** (MIT), reject Remotion (license) & OpenCut (Next.js/rewrite).
  See [../context/decisions.md](../context/decisions.md).
- Vendored to `editor/` (upstream `db6edeb`), installed (426 pkgs), dev server live on :5173
  with correct COOP/COEP. Provenance in `editor/VENDOR.md`.
- Recolored orange → **Cvideo purple `#6d5efc`** (primary, ring, sidebar, playhead); kept dark.
- Full plan: [../editor/freecut-editor.md](../editor/freecut-editor.md).

## 5. Docs organized
- Created `docs/{context,editor,autopilot,sessions,reference}` with an index. Moved
  `SESSION-CONTEXT.md` + `NEXT-SESSION.md` here. Parked Autopilot ideas in
  [../autopilot/content-engine.md](../autopilot/content-engine.md).

## 6. Editor Phase 1a — headless render engine proven
Built FreeCut's `dist`; `node headless/test.mjs` passed every check (Playwright 1.60 + Chrome
already on this machine): render (video, ~3s, real bytes), edit (ops + round-trip captions/
transform/keyframes), edited-project re-render. Confirms the automatable server-side path.

## 7. Editor Phase 2 — embedded same-origin inside Cvideo
FreeCut now runs at `/editor/` inside Cvideo (not a separate tab to a different port): frontend
proxies `/editor` to FreeCut's dev server, FreeCut's router basepath follows `--base=/editor/`,
both documents cross-origin-isolated without conflict, backend tags `Cross-Origin-Resource-
Policy: cross-origin`. New **Editor** sidebar tab. Verified via curl + DOM (nav-item + iframe
render); full WebGPU rendering still needs a real-Chrome check (headless preview can't do it).
**Gap flagged by Dennis:** feels bolted on — opens FreeCut's raw picker, not "my video." Next is
Phase 2b: first-run workspace folder (auto-creates `projects/media/reels`) + "make a video"
auto-opens the editor with scenes loaded, using FreeCut's own scene-detection/caption/edit APIs
(reframed as a mapping job, not a schema build — see [../context/decisions.md](../context/decisions.md)).

## 8. Results — fixed the "Track has no inputs" bug
Root cause: the per-video input panel (`PlatformEditor`) rendered ONCE at the very bottom of the
table, ~911px below whatever row you clicked — with 16 rows, completely out of view, so it
looked like Track did nothing. Fixed: the panel now expands **inline as a row directly under**
the one you clicked (verified 64px gap, `numberInputs: 12` present). `VideoTracker` in `App.tsx`.

## 9. Results — Likes discussed, not added (open question)
Dennis asked why Likes isn't trackable. Answer: deliberate — the app ranks/learns from
**saves + follows** (+ shares) as the "needle" metric; likes are treated like views (vanity,
near-zero predictive value for growth). Views stayed only because it's the rate denominator.
Not yet decided whether to add Likes as an informational (non-scoring) field — would also need
a new Google Sheet column. **Open, needs a decision.**

## 10. Business strategy — pressure-testing a startup wedge (Game B)
Long strategy discussion (not code). Landed on: the **wedge** is the only real unlock; picked
**med spas/aesthetics** as the top candidate to pressure-test (10 real businesses, kill
criterion: <3/10 treat generic-tool mistakes as real pain → wedge dead). Rejected "content
creators" and one-time pricing as traps that quietly re-describe the service path while
claiming to be a startup. Full reasoning + rejected alternatives in
[../context/decisions.md](../context/decisions.md). **Not yet executed** — no list built yet.

## Working tree (uncommitted)
Backend: `db.py`, `main.py` (+ CORP middleware), `.env`. Frontend: `App.tsx` (Studio route +
inline tracker fix), `api.ts`, `Sidebar.tsx`, `vite.config.ts`. New: `editor/` (vendored +
same-origin wiring), `docs/` reorg + this file. `.gitignore`, `.claude/launch.json` updated.
**Nothing committed yet** — also still blocked on the `core.hooksPath` git-hook hijack from
`editor`'s `npm install` (see Next). Nothing lost; all changes are intact on disk.

## Next
1. **Unblock commits**: `editor/`'s `npm install` set the repo's `core.hooksPath` to
   `editor/.vite-hooks/_`, breaking every commit with FreeCut's own pre-commit check. Fix is
   `git config --unset core.hooksPath` — paused because the auto-classifier flagged it as a
   hook-disabling action; needs Dennis to run it or explicitly authorize it. Consider also
   removing/neutralizing `editor/package.json`'s `"prepare": "vp config"` so it can't re-hijack
   on the next `npm install`.
2. **Editor Phase 2b**: first-run workspace folder + auto-open-with-scenes (the actual ask).
3. **Decide on Likes** on Results (informational field, or leave as-is).
4. **Wedge pressure-test**: build the 10-med-spa list + outreach + proof artifact, if/when
   Dennis wants to actually run it.
5. Autopilot stays parked.
