Installing or running this app rather than changing it? Follow [INSTALL.md](INSTALL.md) -
it is written for an agent: every command, the check that proves each step worked, and the
scripts that will hang you if you run them non-interactively.

Changing the code? This project keeps its agent memory in [AGENTS.md](AGENTS.md) - read that
file, then [CONTEXT.md](CONTEXT.md) for how the app actually works.
